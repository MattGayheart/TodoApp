Instructions for use
1) First you want to start the API server located at my other GitHub link and follow the Readme instructions provided there.
2) Run the "startserver.ps1" script using PowerShell and it will automatically install any dependencies and start the server. (This may take a few minutes).
3) The browser will automatically open with the Todo app.